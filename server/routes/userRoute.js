const { Router } = require('express');
const createUser = require("../controllers/userController"); // Adjust the path as needed

const router = Router();

// router.post("/register", createUser);

module.exports = router;
